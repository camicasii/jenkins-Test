pipeline {
  agent any
  tools {
    nodejs 'node-16.19.0'
  }

  options {
    timeout(time: 2, unit: 'MINUTES')
  }

  stages {
    stage('Install dependencies') {
      steps {
        sh 'echo Hola'
      }
    }
    stage('Run tests') {
      steps {
        sh 'echo Hola 2'
      }
    }
  }
}
